# Backend Environment Variables

# Node Environment
NODE_ENV=production

# Server Port
PORT=5000

# Logging
LOG_LEVEL=info

# Data storage
DATA_DIR=./data

# Note: On Render, we mount persistent disk to /app/data
# Make sure the disk mount path is set correctly in Render dashboard
